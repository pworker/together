<p id="notice"><%= notice %></p>
<div class="hero-unit">
  <p>
  <strong><%= t(".Name") %></strong>
  <%= @event.name %>
  </p>

  <p>
  <strong><%= t(".Description") %></strong>
  <%= @event.description %>
  </p>

  <p>
  <strong><%= t(".Start at")%></strong>
  <%= @event.start_at %>
  </p>
  <%= link_to t(".Attendees list"), event_attendees_path(@event), :class =>"btn"%>
  <div class="btn-group">
    <%= link_to t(".Edit"), edit_event_path(@event), :class => "btn" %>
    <%= link_to t(".Back"), events_path, :class => "btn" %>
  </div>
</div>
